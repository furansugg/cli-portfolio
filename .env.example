# Copy to .env and fill in to enable the Last.fm "now playing" badge.
# These VITE_* variables are bundled into the static build, so do NOT use
# anything truly sensitive — Last.fm API keys for read-only public data
# are fine because they only rate-limit access.
VITE_LASTFM_API_KEY=
VITE_LASTFM_USERNAME=
